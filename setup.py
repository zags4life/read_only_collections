# setup.py
from setuptools import setup, find_packages

setup(
    name='read_only_collections',
    version='1.0.0',
    author='Travis Avery',
    python_requires='>=3.7',
    packages=find_packages()
)